<section class="proj" data-index="{{@index}}">
	<span class="proj__icon fa fa-times"></span>
	<div class="proj__container">
		<h3 class="proj__header">{{name}}</h3>

		<div class="markdown">
			{{{content}}}
		</div>
	</div>
</section>